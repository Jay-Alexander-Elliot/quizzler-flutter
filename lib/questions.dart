class Question {
  String questionText; // Non-nullable field
  bool questionAnswer; // Non-nullable field

  // Updated constructor with required keyword
  Question({required this.questionText, required this.questionAnswer});
}
